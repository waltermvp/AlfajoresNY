version: 1
frontend:
  phases:
    preBuild:
      commands:
        - 'npm install -g pnpm'
        - 'pnpm install'
        # - nvm use 20
        # - npm install --silent --global expo-cli
        # - |-
        # if [ -f yarn.lock ]; then
        #  yarn
        # elif [ -f package-lock.json ] || [ -f npm-shrinkwrap.json ]; then
        #  npm ci --cache .npm --prefer-offline
        # else
        #  npm install
        # fi
    build:
      commands:
        - 'tailwindcss -i ./assets/main.css -o ./node_modules/.cache/nativewind/main.css.web.css'
        - 'pnpm run build:production:web'
  artifacts:
    # baseDirectory: /
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
      - $(npm root --global)/**/*
