type Todo @model @auth(rules: [{allow: public, provider: iam}])
{
  content: String
}

type Query {
  purchase(name: String, quantity: Int, productId: String): String @function(name: "FnPurchase") @auth(rules: [{allow: public, provider: iam}])
}